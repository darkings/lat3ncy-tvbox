# Ponyo TV 自动采集、测速与精选订阅完整计划

目标是在 SSH 主机 `jie` 上部署一套长期运行的源管理系统，自动发现、验证、测速和筛选源，最终为 Ponyo TV 提供稳定、高清、无需代理的精选订阅。

核心原则：

- 默认点播源保持 30 个。
- 其中 1 个是聚合后的“儿童动画”。
- 网盘、本地播放、配置中心和电视直播不占 30 个名额。
- 电视直播正式只维护 1 个，后台保留候选。
- 新源必须经过多时段观察才能进入正式订阅。
- 服务器测速与真实电视测试结合。
- GitHub 只发布 APK、订阅、验证工具和 README，不提交 App 源码。

---

## 一、最终产品结构

### 1. 首页

首页默认入口继续使用：

```text
豆瓣推荐
```

它负责内容推荐和检索引导，不参与播放源速度排名，也不占 30 个点播源名额。

### 2. 精选点播源

正式维护 30 个入口：

| 类型 | 数量 |
|---|---:|
| 综合电影、电视剧 | 21 |
| 动漫 | 4 |
| 纪录片 | 2 |
| 综艺 | 2 |
| 聚合儿童动画 | 1 |
| 合计 | 30 |

这是初始配额，后续可以根据测试结果微调。综合源即使同时包含动漫、少儿、纪录片，也只占一个名额，但会带有多个能力标签。

### 3. 不占30个名额的功能入口

单独放进“资源与工具”区域：

- 网盘专用源：2～4 个
- 本地播放：1 个
- Ponyo 设置：1 个
- 电视直播：1 个

这些入口不应混入普通点播源切换列表，避免列表再次变得臃肿。

---

## 二、服务器部署架构

在 SSH 主机 `jie` 上部署以下组件：

```text
公开仓库 / 公开订阅 / 人工提交
                 ↓
          新源采集器
                 ↓
       标准化、去重、安全检查
                 ↓
           候选源数据库
                 ↓
       drpy2 搜索与解析测试
                 ↓
     播放测速、高清检测、稳定性统计
                 ↓
           分类评分与排名
                 ↓
   精选30 / 儿童聚合 / 唯一直播源
                 ↓
       生成订阅并发布到 GitHub
```

建议组件：

- `drpy2`：运行 JS/Python 规则和解析影视信息。
- Python 测试服务：调度、测速、评分和订阅生成。
- SQLite：保存源、测试历史和评分，初期足够。
- `ffprobe`：检查真实分辨率、码率和编码。
- Cron 或 systemd timer：定时执行。
- Docker Compose：如果 `jie` 已安装 Docker，优先容器化。
- GitHub CLI 或 Git：只推送生成文件和报告。

服务器目录可规划为：

```text
ponyo-source-manager/
├─ config/
│  ├─ watch-repos.json
│  ├─ watch-subscriptions.json
│  ├─ allowlist.json
│  ├─ denylist.json
│  └─ policy.json
├─ data/
│  ├─ sources.db
│  └─ cache/
├─ reports/
├─ output/
├─ scripts/
└─ logs/
```

GitHub Token、Cookie 等敏感信息只能放在服务器环境变量中，不写进仓库。

---

## 三、新源获取方案

### 1. 来源渠道

采集器从以下位置获取候选源：

- 维护活跃的 TVBox 规则仓库
- drpy、drpy2、hipy 规则仓库
- 已验证公开订阅
- 当前 175 个源的上游依赖
- 上游仓库的新提交和替代分支
- 人工提交的规则地址
- 历史失效源的新版本

### 2. 增量采集

每 6 小时检查一次：

- 仓库是否有新提交
- JSON/JS/Python 规则是否变化
- 订阅中是否增加新 `sites`
- 已有规则是否更换 API 或域名
- 失效源是否恢复

使用提交哈希、ETag 和文件哈希避免重复下载。

### 3. 新源隔离

新发现的源只写入：

```text
candidates.json
```

不会直接进入正式订阅。候选源至少观察 3 天，推荐观察 7 天。

---

## 四、源标准化和去重

不同订阅经常复制同一个源并修改名称，因此不能只按名称去重。

去重指纹包含：

- `api` 地址
- `ext` 规则地址
- 规则文件 SHA-256
- API 主机名
- 搜索请求格式
- 播放解析域名
- 返回内容相似度
- JAR 文件哈希
- 爬虫类名

两个名称不同但规则、接口和内容高度相同的源，只保留稳定性更高的一个。

统一显示名称建议使用：

```text
类型｜简短名称
```

例如：

```text
影视｜星辰
动漫｜次元
纪录｜视界
综艺｜欢乐
网盘｜夸克
```

名称不包含“推荐、极速、超清、蓝光”等未经验证的宣传词。

---

## 五、内容分类和过滤政策

### 1. 默认保留

- 电影
- 电视剧
- 动漫
- 儿童动画
- 纪录片
- 综艺

### 2. 排除内容

以下内容不进入正式精选点播源：

- 戏曲专源
- 京剧、越剧、豫剧等专源
- 广场舞专源
- 成人内容
- 大量博彩、色情或诱导下载广告
- 纯接口测试源
- 纯推送工具源
- 强制代理源
- 包名保护或设备绑定源
- 会触发 App 退出的 JAR

综合源中包含少量戏曲分类时，不必删除整个源，可以隐藏相关分类。

建议屏蔽分类：

```text
戏曲
京剧
越剧
豫剧
黄梅戏
广场舞
成人
伦理
```

### 3. 功能型入口单独归类

以下条目不参与点播源排名：

- 本地播放
- 配置中心
- 推送
- 网盘
- Cookie 设置
- 扫码登录
- 电视直播

---

## 六、分层验证流程

每个候选源依次经过六层测试。

### 第一层：配置语法

检查：

- JSON 是否有效
- JS/Python 规则能否加载
- 必填字段是否存在
- 编码是否正常
- 是否存在无效转义
- JAR MD5 是否匹配
- 依赖地址能否下载

不通过时立即隔离。

### 第二层：安全检查

扫描：

- 强制退出代码
- 包名验证
- `killProcess`
- `System.exit`
- 外部命令
- 可疑文件写入
- 私有服务器上传
- Cookie 或 Token 泄露
- 未知加密 JAR
- `127.0.0.1`、`localhost` 等本地依赖

存在高风险行为时写入 `denylist.json`。

### 第三层：无代理连通性

在服务器明确不使用代理的情况下测试：

- DNS 解析
- TCP 建连
- TLS 证书
- 首页响应
- API 响应时间
- 规则依赖下载
- 播放域名访问

普通精选源必须无代理可访问。

### 第四层：业务功能

通过 drpy2 实际执行：

- 首页分类
- 搜索
- 详情
- 选集
- 播放地址解析

测试词应覆盖：

- 热门电影
- 热门电视剧
- 老影片
- 国产动漫
- 儿童动画
- 纪录片
- 综艺节目

仅首页能打开、搜索和详情失败的源不能进入精选订阅。

### 第五层：实际播放

不能只检查播放 URL 是否返回 HTTP 200，还要验证：

- 播放列表可解析
- 视频分片真实存在
- 连续读取 2～3 个分片
- 跳转地址有效
- 所需请求头是否完整
- 防盗链是否导致电视播放失败
- 播放地址是否过快过期

### 第六层：媒体质量

使用 `ffprobe` 检查：

- 实际分辨率
- 视频码率
- 视频编码
- 音频编码
- 帧率
- HLS 多档清晰度
- 分片稳定性

---

## 七、高清判断标准

不相信标题中的“高清、1080P、4K、蓝光”，只看实际播放流。

| 等级 | 实际分辨率 | 建议码率 |
|---|---|---:|
| 标清 | 低于 720p | 不限 |
| 高清 | 720p | ≥ 1.5 Mbps |
| 全高清 | 1080p | ≥ 3 Mbps |
| 超高清 | 2160p | ≥ 10 Mbps |

源的高清准入条件：

- 抽测内容中 80% 以上达到 720p
- 50% 以上达到真实 1080p
- 不能长期使用极低码率伪装 1080p
- 优先兼容性好的 `H.264 + AAC`
- HEVC 可以保留，但需确认电视支持硬解
- AV1 不作为旧电视的默认线路

老动画、老电影按原始片源质量处理，不会因为无法达到 1080p 直接判定失效。

---

## 八、多时段测速安排

每天至少测试四次：

```text
08:00
13:00
20:00
23:00
```

其中 `20:00` 和 `23:00` 属于晚间高峰，权重更高。

### 快速检测

每 6 小时运行：

- 首页
- 搜索
- 详情
- 播放地址
- 首个分片
- 基础响应时间

### 深度检测

每天运行一次：

- 每个源抽测 3～5 个内容
- 多集连续解析
- 真实分辨率和码率
- 连续分片读取
- 线路过期情况
- 失败原因分类

### 长期统计

保存最近 14 天记录：

- 24 小时数据用于即时告警
- 7 天数据用于精选排名
- 14 天数据用于稳定性趋势

---

## 九、普通点播源评分

总分 100 分：

| 指标 | 权重 |
|---|---:|
| 播放成功率 | 35 |
| 多时段稳定性 | 25 |
| 首帧与读取速度 | 20 |
| 搜索、详情、选集成功率 | 10 |
| 高清比例与内容质量 | 10 |

硬性准入条件：

- 搜索和详情成功率 ≥ 90%
- 播放成功率 ≥ 85%
- 最近三天无连续严重故障
- 首帧中位时间建议小于 4 秒
- 720p 比例 ≥ 80%
- 无代理可访问
- 不存在强制退出和包名保护问题

速度统计同时保存：

- 中位数 `P50`
- 较差时段 `P95`
- 晚间高峰速度
- 连续失败次数

不能只用平均值，否则偶尔特别快的结果会掩盖高峰期卡顿。

---

## 十、精选30的晋级和淘汰

### 新源晋级

新源必须：

1. 完成静态安全检查。
2. 连续通过至少三个不同时段。
3. 观察不少于 3 天。
4. 推荐完整观察 7 天。
5. 综合分高于当前正式源。
6. 没有代理、登录或本地服务依赖。

### 正式源淘汰

正式源出现以下情况时进入降级观察：

- 连续两个时段播放失败
- 三天播放成功率低于 80%
- 晚间高峰持续超时
- 内容大面积失效
- 上游加入包名保护
- 出现不安全代码或恶意跳转

### 防止频繁变化

- 每天最多替换 3 个普通源
- 新源连续合格后才晋级
- 当前源连续失败后才淘汰
- 同一套接口最多保留一个主要副本
- 每次发布保留上一版本用于回滚

---

## 十一、儿童动画聚合源

### 1. 用户界面

用户只看到一个入口：

```text
儿童动画
```

或者采用品牌化名称：

```text
Ponyo 少儿
```

推荐使用 `儿童动画`，含义更直接。

它在精选 30 中只占一个名额。

### 2. 后台组成

聚合源内部接入：

- 至少 2 个主力儿童专源
- 至少 2 个备用儿童专源
- 若干综合源中经过验证的少儿分类

底层源不单独显示，也不重复占 30 个名额。

### 3. 统一分类

```text
热门推荐
学龄前
国产动画
经典动画
少儿英语
动画电影
科普启蒙
```

### 4. 搜索和去重

使用以下字段判断是否为同一内容：

```text
标准化标题
年份
季数
集数
语言
版本
```

不能只按标题合并，避免将同名动画、国语版和英文版错误混在一起。

### 5. 播放线路选择

后台为每一集保存多个来源：

```text
动画标题
  └─ 第1集
      ├─ 高清线路1
      ├─ 高清线路2
      └─ 备用线路
```

选择顺序：

1. 真实分辨率
2. 近期播放成功率
3. 首帧速度
4. 晚间稳定性
5. 电视编码兼容性

主线路失败时，只能切换到标题、季数、集数和语言一致的备用线路。

### 6. 儿童内容安全

儿童聚合源需要额外检查：

- 分类中没有成人内容
- 没有明显博彩和不良广告
- 不弹出外部浏览器
- 不要求儿童扫码或填写账号
- 不使用诱导下载页面
- 优先国语和清晰配音版本

---

## 十二、网盘源处理

网盘源可以保留，但不占精选 30。

建议初期保留：

```text
网盘｜阿里
网盘｜夸克
网盘｜综合
```

必要时增加：

```text
网盘｜115
```

网盘源验证内容：

- 规则能否加载
- 登录配置是否正常
- Cookie 缺失时是否正确提示
- 配置后是否能获取文件
- 解析失败是否会导致 App 退出
- 是否存在账号或 Cookie 泄露

用户 Cookie 应优先保存在电视本地，不写入远程订阅、不上传 GitHub。服务器不能使用用户真实 Cookie 做公共测速。

网盘源不按普通播放成功率淘汰，只进行可用性和安全性检测。

---

## 十三、本地播放

只保留一个入口：

```text
本地播放
```

要求：

- 支持电视内部存储
- 支持 USB 移动存储
- 权限不足时正常提示
- 文件不存在时不黑屏
- 返回后不重新刷新整个配置
- 不参与任何网络测速
- 不成为首页默认源

---

## 十四、配置中心

只保留一个入口：

```text
Ponyo 设置
```

建议管理：

- 网盘 Cookie
- 播放器选择
- 硬解/软解
- 订阅刷新
- 清理缓存
- 网络诊断
- 源健康状态
- 恢复内置订阅

配置中心放在工具区末尾，不进入影视源列表，不参与排名。

---

## 十五、电视直播

### 1. 发布策略

用户端只维护一个正式直播源。

后台维护 3～5 个直播候选，自动测速竞争。只有最高分的一个写入正式配置。

### 2. 测试频道

至少抽测：

- CCTV-1
- CCTV-5
- CCTV-6
- CCTV-8
- CCTV-13
- CCTV-14
- 常见卫视频道
- 可用的少儿卫视频道

CCTV-14 等少儿频道设为重点检查项。

### 3. 测试指标

- 播放列表是否可下载
- 频道总数
- 重复频道比例
- 主流频道有效率
- 首帧时间
- 连续播放 60 秒是否断流
- HLS 分片是否持续更新
- 晚间高峰稳定性
- EPG 和台标是否可用
- 是否需要代理

### 4. 直播评分

| 指标 | 权重 |
|---|---:|
| 主流频道有效率 | 35 |
| 多时段稳定性 | 30 |
| 首帧速度 | 20 |
| 清晰度与码率 | 10 |
| EPG、台标和频道整理 | 5 |

正式直播源建议满足：

- 主流频道有效率 ≥ 90%
- 多时段成功率 ≥ 85%
- 首帧中位时间小于 5 秒
- 无代理可访问
- 连续三个时段合格后才能替换当前源

---

## 十六、GitHub加速和订阅容灾

App 读取订阅时采用三级回退：

```text
1. jsDelivr 加速地址
2. 经过验证的 GitHub Raw 加速地址
3. APK 内置精选订阅
```

主订阅地址保持类似：

```text
https://cdn.jsdelivr.net/gh/darkings/lat3ncy-tvbox@main/subscription/ponyo-lite.json
```

每次测速时同时验证：

- GitHub Raw
- jsDelivr
- 当前备用加速站
- DNS 与 TLS
- 下载时间
- 文件 SHA-256
- JSON 是否完整

不能仅根据 HTTP 200 判断加速站正常，必须确认文件内容和哈希一致。

发布时生成版本清单：

```json
{
  "version": "20260724-001",
  "commit": "git-commit-id",
  "sha256": "配置文件哈希",
  "generated_at": "生成时间",
  "source_count": 30
}
```

---

## 十七、生成文件设计

建议生成：

```text
subscription/
├─ ponyo-lite.json
├─ ponyo-full.json
├─ ponyo-live.json
├─ ponyo-children.json
├─ ponyo-candidates.json
└─ manifest.json
```

用途：

- `ponyo-lite.json`：Ponyo TV 默认配置。
- `ponyo-full.json`：完整备用源。
- `ponyo-live.json`：唯一正式直播配置。
- `ponyo-children.json`：儿童聚合源后台配置。
- `ponyo-candidates.json`：候选记录，可选择不公开全部细节。
- `manifest.json`：版本、哈希和发布时间。

健康报告：

```text
reports/
├─ source-health.json
├─ source-ranking.json
├─ live-health.json
├─ children-health.json
└─ rejected-sources.json
```

公开报告不得包含 Cookie、Token、完整播放临时地址或其他敏感信息。

---

## 十八、发布和回滚

发布前必须通过：

- JSON Schema 校验
- 所有远程依赖下载验证
- JAR MD5/SHA-256 校验
- 30 个精选源数量检查
- 儿童聚合至少 2 个主力可用
- 直播源主流频道检查
- 加速地址内容一致性检查
- APK 内置回退配置检查

发布策略：

1. 先生成临时文件。
2. 完成全部验证。
3. 原子替换正式文件。
4. Git 提交。
5. 推送 GitHub。
6. 验证 CDN 文件内容。
7. 保存发布结果。

保留最近 7 个正式版本。新版本出现严重问题时，立即回退上一份配置。

---

## 十九、Ponyo TV App调整

后台稳定后再构建 Beauty 2.5。

App 需要调整：

- 首页保持“豆瓣推荐”。
- 普通点播源列表只显示精选 30。
- 增加统一“儿童动画”入口。
- 网盘、本地和配置中心放入资源/工具区域。
- 电视直播使用独立菜单。
- 配置加载失败自动使用内置订阅。
- 搜索、详情、选集失败时不能退出 App。
- 返回操作不能黑屏。
- 远程配置失败不能无限刷新。
- 保留 Beauty 2.4 的 JAR 包名保护修复。
- 保留 Logo Splash。
- 保持宠物当前尺寸和 240ms 动画速度，除非后续另行调整。

仓库继续遵守：

- 提交 APK
- 提交订阅文件
- 提交验证和测速工具
- 提交根目录 README
- 不提交 App 源码

---

## 二十、真实电视验证

服务器测试通过不代表用户家中电视网络一定相同，因此最终必须在真实电视验证。

测试项目：

- 冷启动
- 配置加载
- 首页推荐
- 切换 30 个源
- 儿童动画搜索和播放
- 儿童线路自动切换
- 普通搜索
- 详情和选集
- 连续播放
- 直播切台
- 网盘未配置提示
- 本地文件权限
- 返回键行为
- 断网恢复
- CDN 失败回退
- JAR 加载
- 长时间待机恢复

建议分别在：

```text
白天
晚间20:00～23:00
断网后恢复
App后台恢复
电视重启后首次启动
```

进行验证。

第一阶段不收集用户观看记录。可以增加“一键网络诊断”，只导出：

- 源响应时间
- 播放成功或失败
- 首帧时间
- 编码兼容性
- 错误类型

不上传搜索词、片名、账号和 Cookie。

---

## 二十一、实施时间表

### 阶段一：环境确认，约半天

- SSH 登录 `jie`
- 检查系统、磁盘和网络
- 检查 Docker、Python、Node、ffmpeg
- 检查 GitHub 推送权限
- 确定服务运行目录
- 确认服务器没有代理环境变量

### 阶段二：部署drpy2和数据库，约1天

- 部署 drpy2
- 创建 SQLite 数据库
- 导入当前 175 个源
- 建立标准化和去重流程
- 建立候选、允许和拒绝列表

### 阶段三：完成自动测试，约1～2天

- 搜索、详情、选集测试
- 播放解析
- HLS 分片测速
- ffprobe 高清检测
- 定时任务
- 健康报告

### 阶段四：儿童聚合与直播，约1～2天

- 儿童内容识别
- 多源去重
- 多线路选择和故障切换
- 直播候选池
- 直播频道测速

### 阶段五：观察期，至少7天

- 四个时段持续测速
- 修正规则误判
- 排除伪高清
- 检查晚间稳定性
- 确定精选 30
- 确定唯一正式直播源

### 阶段六：发布订阅，约半天

- 生成正式配置
- 发布 GitHub
- 验证 CDN 和备用加速地址
- 验证内置回退配置

### 阶段七：构建Beauty 2.5并实机测试，约1天

- App 默认指向精选订阅
- 接入儿童聚合
- 调整资源与工具入口
- 构建通用 APK
- 在真实电视完成验证
- 只提交 APK 和订阅相关文件

---

## 二十二、最终验收标准

系统完成后应达到：

- 精选点播入口严格保持 30 个。
- 其中包含一个多源聚合的儿童动画入口。
- 儿童聚合至少有 2 个主力和 2 个备用源。
- 网盘、本地、配置和直播不占 30 个名额。
- 只向用户提供一个正式直播源。
- 普通源无需代理。
- 正式源播放成功率不低于 85%。
- 搜索和详情成功率不低于 90%。
- 80% 以上抽测内容达到真实 720p。
- 50% 以上抽测内容达到真实 1080p。
- 晚间高峰有独立评分。
- 新源经过至少 3 天观察，推荐 7 天。
- 配置更新失败时自动回退。
- 搜索或播放失败不会导致 App 退出、黑屏或无限刷新。
- GitHub 不包含 App 源码和任何账号凭据。
